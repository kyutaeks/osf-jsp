자바스크립트에서는 function 이라고 부른다. 앞에 window.이 생략된것이다.
객체지향 기반 스크립트. 

-var
변수 선언가능

-function
함수 선언

변수 선언할때 무조건 var을 써줘야한다.

java script 는 데이터타입을 구분하지않는다.
boolean : true; double : 1.1; String : "str"; char : 'str'; 등 구분없이 쓸수있다.

그리고 ''를 사용하는것이 규칙이다.

id는 화면안에서만 쓰는것이고 중복 X 
name 은 서버에 전송 중복 O

404 not found 서버에 연결은 되었지만 잘못된 질문을 했을때.

BOM -> 최상위 모델 window
DOM -> 최상위 모델 document

?를 기준으로 왼쪽이 요청하는 주소 .
?를 기준으로 오른쪽이 키 밸류.

post 방식 : 주소값이 바뀌지않는다
데이터 신규생성할때 많이 사용한다.

get 방식 : 주소창에 노출이 되지않는다.
조회할때 많이 사용한다.

<form>태그에서 뒤에 post라고 써주지않으면 디폴트는 get방식.
<form method ="get">

java script의 파라미터는 넣어도 그만 안넣어도 그만.








